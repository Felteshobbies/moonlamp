"""Moon lamp firmware.

The version is read by the web interface and by tools/status.py, so a lamp can
always say which build is on it -- there is no other way to tell once the Pico
is on the wall.
"""

VERSION = "0.2"
